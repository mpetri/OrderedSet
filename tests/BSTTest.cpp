#include "TestHarness.h"

#include "BinarySearchTree.h"
#include "LinkedList.h"

#include <stdlib.h>


TEST(BST , InsertFind)
{
    CHECK(false);
}

TEST(BST , Remove)
{
    CHECK(false);
}

TEST(BST , RemoveRoot)
{
    CHECK(false);
}


TEST(BST , Min)
{
    CHECK(false);
}

TEST(BST , Max)
{
    CHECK(false);
}

TEST(BST , Successor)
{
    CHECK(false);
}
